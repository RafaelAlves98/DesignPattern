package designpattern;

// Classe que representa a memória do computador.
class Memoria {
    // Método que simula o carregamento de dados na memória.
    public void load(long posicao, byte[] data) {
        System.out.println("Carregando dados na memória a partir da posição: " + posicao);
    }
}

